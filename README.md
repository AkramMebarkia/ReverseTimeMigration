# ReverseTimeMigration
Reverse Time Migration (RTM) uses numerical wave propagation to generate high-resolution seismic images.  Finite-difference stencil updates dominate the computational workload and map efficiently to GPUs.
